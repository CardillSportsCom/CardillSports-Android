package com.cardill.sports.stattracker.common.data;

public enum PlayerStatType implements StatType {
    WINS,
    GP,
    FG_PERCENT,
    POINTS,
    THREES,
    AST,
    REB,
    STL,
    BLK,
    TO
}
